<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menuPhoto_Camera"
        android:title="Camera"
        android:icon="@drawable/ic_camera"
        app:showAsAction="always"
        android:iconTint="?attr/colorOnBackground"/>

    <item android:id="@+id/menuPhoto_Search"
        android:title="Search"
        android:icon="@drawable/ic_search"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        android:iconTint="?attr/colorOnBackground"/>

    <item android:id="@+id/menuPhoto_Choose"
        android:title="MultiSelect"
        app:showAsAction="never"/>

    <item android:id="@+id/menuPhoto_Settings"
        android:title="Setting"
        app:showAsAction="never"/>

</menu>
